[project]
name = "text-to-speech"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Basir Naji", email = "basirnaji007@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "gtts>=2.5.4",
    "questionary>=2.1.1",
    "rich>=15.0.0",
]

[project.scripts]
text-to-speech = "text_to_speech.main:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
